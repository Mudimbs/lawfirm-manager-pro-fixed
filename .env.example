# Server
PORT=3000
SESSION_SECRET=change_me_now

# SMTP (optional - for audience reminder emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Cabinet <no-reply@cabinet.local>"
